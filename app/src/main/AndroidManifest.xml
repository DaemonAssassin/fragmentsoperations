<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gmail.mateendev3.frags">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Frags">
        <activity
            android:name=".activities.ScreenSizeRes2Activity"
            android:exported="false" />
        <activity
            android:name=".activities.ScreenSizeResActivity"
            android:exported="false" />
        <activity
            android:name=".activities.CalculateScreenSizeActivity"
            android:exported="false" />
        <activity
            android:name=".activities.SendDataToFragment"
            android:exported="false" />
        <activity
            android:name=".activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.AddRemoveFragment"
            android:exported="false" />
    </application>

</manifest>